apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monitoring-components
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/mitch-ross/k3s-argocd-starter.git # Change this to your repo
        revision: HEAD
        directories:
          - path: monitoring/argocd/applications/*
  template:
    metadata:
      name: '{{ .path.basename }}'
      labels:
        type: monitoring
    spec:
      project: monitoring
      source:
        repoURL: https://github.com/mitch-ross/k3s-argocd-starter.git # Change this to your repo
        targetRevision: HEAD
        path: '{{ .path.path }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: monitoring
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false # The namespace is created separately
          - ServerSideApply=true # Recommended for CRDs 
